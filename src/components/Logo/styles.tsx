import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
    },
    image: {
        maxHeight: 180,
        objectFit: "contain"
    },
    title: {
        textAlign: "center",
        fontWeight: "bold",
        fontSize: 25,
        color: "#FABF01"
    }
})
